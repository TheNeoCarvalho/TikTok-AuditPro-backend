app = "tiktok-audit-backend"
primary_region = "gru" # São Paulo (mude para sua preferência)

[build]

[http_service]
  internal_port = 3001
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0
  processes = ["app"]

[env]
  PORT = "3001"

[[vm]]
  size = "shared-cpu-1x"
  memory_mb = 512
